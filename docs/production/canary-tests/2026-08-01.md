# Production canary evidence — 2026-08-01

## Classification

`WARN` — the safe production workflow and hosted AI provenance passed. The only
canary warning was the intentional paid-media skip. This evidence supports the
controlled beta; it does not remove the separate PITR/public-acquisition block.

## Scope

- Operator: Codex, using a short-lived normal production web session supplied
  by the account owner
- Identity type: dedicated non-admin synthetic Auth0 user
- Workspace: `[SYNTHETIC CANARY] Squadpitch Production Canary`
- Workspace identifier: `cmsa…9sjq` (redacted)
- API release source: `b81951e`
- Run identifier: `production-canary-20260801-…3978` (redacted)
- Request/trace family: `production-canary:production-canary-20260801-…3978`
- Start: 2026-08-01 14:19:38 UTC
- End: 2026-08-01 14:19:40 UTC
- Observed wall duration: approximately 2 seconds

No access token, cookie, password, signing key, Auth0 subject, customer content,
or provider credential is retained in this record.

## Production results

| Evidence | Result | Production observation |
| --- | --- | --- |
| Normal authentication and authorization | PASS | Normal workspace-owner middleware admitted only the dedicated synthetic workspace. |
| Exact workspace allowlist | PASS | The configured workspace and required `[SYNTHETIC CANARY]` marker matched. |
| Postgres | PASS | A marked `WorkspaceDataSource` was written/read inside a transaction and forcibly rolled back. |
| Billing boundary | PASS | Entitlement status was read; no checkout, subscription, invoice, or charge operation ran. |
| BullMQ/worker | PASS | One dedicated job was enqueued, consumed, and removed from `sp-production-canary`. |
| Hosted AI path | PASS | Four of five usable dry-run operations returned `source=squadpitch-ai`. |
| Provenance | PASS | All five operations returned provenance; the remaining Brand Quality operation truthfully reported Node shadow execution. |
| Trace correlation | PASS | All five Node provenance trace IDs correlated with their signed-envelope operation trace. |
| Fallback | PASS | Explicit fallback classification was present; no fallback was used during the run. |
| AI persistence | PASS | Action Proposal ran with `dryRun=true` and `persistence=false`; no draft was created. |
| Sites | PASS | Production SquadSites health returned HTTP 200. |
| Publishing/messaging | PASS | No publishing adapter, email sender, or SMS sender was invoked. |
| Provider boundary | PASS | Configuration presence only was inspected; no OAuth or provider action ran. |
| Media | WARN | Paid media generation remained disabled and was not attempted. |

Final canary summary: **11 PASS, 1 WARN, 0 FAIL**.

## Cleanup

- The database probe was transactionally rolled back.
- The BullMQ job used `removeOnComplete`/`removeOnFail`; the completed job was
  consumed and removed, and producer/consumer connections were closed.
- AI inputs were fixed synthetic data and all operations were read-only,
  proposal-only, or explicit non-persistent dry runs.
- No recurring job was created.
- No public site, social post, email, SMS, Stripe object, or customer record was
  created or modified.
- The dedicated canary identity/workspace remains in place for future runs, as
  required by the runbook.

## Readiness evidence output

The authenticated production-readiness run reported PASS for all nine distinct
live-evidence checks:

- `CANARY_IDENTITY_CONFIGURED`
- `CANARY_WORKSPACE_ALLOWLISTED`
- `CANARY_DATABASE_PATH_VERIFIED`
- `CANARY_QUEUE_PATH_VERIFIED`
- `CANARY_AI_PATH_VERIFIED`
- `CANARY_CLEANUP_VERIFIED`
- `AI_PROVENANCE_PRESENT`
- `AI_HOSTED_SERVICE_VERIFIED`
- `AI_FALLBACK_AVAILABLE`

The broader verifier remained `NOT_READY` when run from the operator workstation
because production-only configuration and private database/worker connectivity
were intentionally unavailable locally. Those unrelated local probe failures do
not override the live canary evidence above.

## Remaining warnings

- Paid media remains outside the automatic canary.
- Twilio is partially configured and its authenticated local probe returned 401;
  SMS sending remains disabled.
- Pinterest readiness remains subject to its separate production approval.
- Snapshot restoration is proven, but PITR remains unavailable; public
  acquisition remains blocked under the accepted recovery policy.
