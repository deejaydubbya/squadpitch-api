# Production SMS disablement evidence — 2026-08-01

- Classification: **WARN — intentionally unavailable**
- Operator: Codex, acting for the Squadpitch production safety review
- Capability endpoint: HTTP 200, `status=unavailable`,
  `availability=disabled`, `reason=twilio_account_suspended`
- Direct test-send endpoint: HTTP 503, `SMS_UNAVAILABLE`
- Customer message: `SMS is temporarily unavailable.`

## Queue aggregates

The production `sp-notification` queue was inspected by job name only. No job
payload, recipient, phone number, or message body was read or recorded.

| State      | SMS jobs |
| ---------- | -------: |
| Waiting    |        0 |
| Delayed    |        0 |
| Active     |        0 |
| Failed     |        0 |
| Retrying   |        0 |
| Repeatable |        0 |

No jobs required deletion. No conversation or message history was changed.

## Readiness evidence

- `TWILIO_ACCOUNT_AVAILABLE`: WARN — intentionally unavailable
- `SMS_CAPABILITY_AVAILABLE`: WARN — disabled
- `SMS_SEND_PATH_BLOCKED`: PASS
- `SMS_INBOUND_SIDE_EFFECTS_BLOCKED`: PASS
- `SMS_JOBS_DISABLED`: PASS
- `SMS_UI_ACCURATE`: PASS
- `SMS_AI_ACTIONS_BLOCKED`: PASS
- `SMS_SECRETS_REMOVED_OR_QUARANTINED`: PASS
- `twilio.connectivity`: WARN — intentionally not probed

The broader local readiness invocation reported `43 PASS | 5 WARN | 1 BLOCKED |
6 FAIL` because the local verifier environment does not mirror several production
configuration and connectivity values. The authenticated SMS evidence above came
from the deployed production API and is not inferred from credential presence.

## Secret handling

Secret names were inspected without values. Outbound-only
`TWILIO_ACCOUNT_SID` and `TWILIO_FROM_NUMBER` were removed from the API Fly app.
`TWILIO_MESSAGING_SERVICE_SID` was absent. `TWILIO_AUTH_TOKEN` remains quarantined
solely so invalid inbound and status callbacks continue to fail signature
validation. `SMS_SENDING_ENABLED=false` and `SMS_A2P_APPROVED=false` are explicit
production settings.

No Twilio dashboard setting, customer data, email path, or non-SMS inbox behavior
was modified during verification.
