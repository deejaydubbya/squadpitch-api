# Production worker-health evidence — 2026-08-01

## Result

- Classification: **PASS**
- Operator: Codex, acting for the Squadpitch production-readiness review
- Verification window: 2026-08-01 14:51–15:11 UTC
- Final synthetic correlation: `worker-health-racefix-1785596672560-3`
- Post-fix queue round-trips: 3/3 PASS (329 ms, 519 ms, and 312 ms)
- Cleanup: PASS; the correlated synthetic job was removed

No customer payload, message body, authentication value, or production customer
identifier was inspected or recorded.

## Heartbeats and Redis

- Redis PING: PASS
- Worker processes observed by fresh heartbeat: 3
- Expected services observed: `api-worker` and `squadpitch-ai-worker`
- Heartbeat freshness: PASS (five-minute critical threshold)
- Heartbeat interval / expiry: 30 seconds / 360 seconds

The three instances represent two API worker-group machines and one dedicated AI
worker. Heartbeats contain only timestamp, service, instance, release, and status.

## Queue evidence

Queues inspected using aggregates only:

- `sp-media-gen`
- `sp-video-gen`
- `sp-persona-training`
- `sp-notification`
- `sp-metrics-sync`
- `sp-analytics-recalc`
- `sp-insights-refresh`
- `sp-scheduled-publish`
- `sp-weekly-digest`
- `sp-gbp-review-poll`
- `sp-worker-health`

Final production observations:

| Signal                                 | Result | Observed | Warning / critical threshold |
| -------------------------------------- | ------ | -------: | ---------------------------- |
| Waiting backlog                        | PASS   |        0 | 25 / 100 jobs                |
| Oldest waiting age                     | PASS   |     0 ms | 5 / 15 minutes               |
| Failed jobs, prior 15 minutes          | PASS   |        0 | 3 / 10 jobs                  |
| Stalled jobs, prior 15 minutes         | PASS   |        0 | any / repeated               |
| Retry-exhausted jobs, prior 15 minutes | PASS   |        0 | any / repeated               |

Active and delayed counts and the oldest delayed age are inspected by the check,
but delayed jobs do not alert because publishing and polling legitimately schedule
work for the future.

## Synthetic consumption

The authenticated production canary enqueued a dedicated
`[SYNTHETIC CANARY] worker-health` job. The standing API worker group consumed it,
preserved the unique correlation, acknowledged it, and removed it. Three consecutive
post-fix production checks passed in 329 ms, 519 ms, and 312 ms. The check made no
provider call and did not exercise publishing, messaging, or billing.

Two checks exposed a QueueEvents subscription race: the standing worker could
finish the tiny job before the verifier's event listener became ready. The verifier
now establishes its event subscription before enqueueing. Redis, heartbeat,
backlog, failure, stall, and retry signals remained healthy while this was fixed.

## Sentry alert probe

- Command: `npm run verify:worker-alert` inside `squadpitch-api`
- Emission: PASS
- Sentry project: Squadpitch API
- Event ID: `2ae3ed318190415db83fb6b389ba8738`
- Tags: `synthetic=true`, `source=worker-health`, `environment=production`
- Delivery status: PASS; the event was manually confirmed in the Squadpitch API
  project and the alert email was received

The command emitted one safe event. It did not create a failed queue job or stop a
worker. Incident grouping and a 15-minute Redis cooldown protect real alerts from
flooding.

## Remaining limitations

- `WORKER_ALERT_DELIVERY_VERIFIED=true` was applied through the existing Fly
  production secret path after manual event and email confirmation.
- The single transient round-trip failure should be investigated if it recurs; the
  final isolated production check and all standing health signals passed.
- Thresholds are deliberately conservative for controlled-beta traffic and should
  be recalibrated after enough production history is available.

## Automated readiness consumption

The broader production verifier is configured to consume the existing public,
coarse `GET /ready` response through `SQUADPITCH_WORKER_HEALTH_URL`. The endpoint
requires no credential and exposes only service readiness plus database/Redis
booleans and the aggregate worker state. The verifier uses a bounded timeout,
validates `squadpitch-api` readiness schema v1, and does not print the endpoint.
Detailed queue evidence remains restricted to the authenticated synthetic canary.

Post-deployment verification at `2026-08-01T18:47:16.392Z`:

- Endpoint configured: PASS
- Endpoint reachability: PASS (HTTP 200)
- Safe response schema: PASS
- Aggregate production worker state: PASS (`healthy`)
- Broader readiness result: `READY_WITH_WARNINGS` with 36 PASS, 3 WARN,
  19 BLOCKED and 0 FAIL

The remaining warnings are the intentionally disabled Twilio/SMS capability and
its intentionally skipped connectivity probe. The blocked items require the
separate authenticated production-canary session evidence and do not represent
an automated worker-health URL or worker-state failure.
