# Production backup audit — 2026-07-31

## Outcome

This is durable evidence of a read-only provider audit, **not a completed restore test**. Recovery readiness is blocked because WAL/PITR backups are disabled and no backup has been restored and validated. No production data, credentials, or customer content was accessed or recorded.

## Postgres evidence

| Field                      | Observed evidence                                                                |
| -------------------------- | -------------------------------------------------------------------------------- |
| Provider                   | Unmanaged Fly Postgres 17.2, single primary in `ord`                             |
| Storage                    | One encrypted 3 GB Fly volume                                                    |
| Backup source              | Fly volume snapshots                                                             |
| Latest successful snapshot | 2026-07-31 06:13:58 UTC                                                          |
| Frequency/retention        | Daily snapshots; five days visible; retention reported as five days              |
| PITR                       | Not configured; `fly postgres backup list` reported backups disabled             |
| Earliest restorable point  | Not established; WAL/PITR unavailable                                            |
| Backup location            | Not exposed by the inspected CLI metadata                                        |
| Restore-to-new-target      | Requires provisioning an isolated, billable target; not authorized in this audit |

Audit start/end: 2026-07-31 (read-only CLI checks). Operator: Codex acting under the account owner's authorization.

## Restore record

- Source restore point: none selected.
- Temporary target: not created.
- Restore duration: not measured.
- Schema, migration, row-count, relationship, corruption, and application-connectivity checks: not run.
- Cleanup: none required because no resources were created.
- Proven RPO: no end-to-end RPO is proven. The observed snapshot cadence limits Postgres backup-point granularity to approximately 24 hours, subject to snapshot usability that remains untested.
- Proven RTO: none.

An actual test requires explicit approval to provision a restricted temporary database/volume, restore a snapshot, run aggregate-only read checks with all workers and outbound actions disabled, and then separately approve cleanup.

## Other production stores

| System                             | Authority and recovery finding                                                                                                                                                                                                              | Impact if lost                                                                              |
| ---------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------- |
| Postgres                           | Application system of record. Daily encrypted-volume snapshots exist; restore and PITR remain unproved.                                                                                                                                     | Workspaces, content, schedules, billing mirrors, integrations, audit and idempotency state. |
| Upstash Redis/BullMQ               | Cache/rate/nonce state is disposable; delayed, retry and repeat queue state is operationally durable. Backup configuration was not accessible. Rebuild/reconcile queues from Postgres; do not blindly replay a stale snapshot.              | In-flight OAuth restarts; jobs may be delayed, missing, or duplicated until reconciled.     |
| Cloudinary                         | Original media bytes are authoritative outside Postgres; Postgres has metadata/public IDs only. Read-only usage showed 3,467 resources and about 655 MiB on the Free plan. Backup/versioning/trash policy was not exposed by the API audit. | Missing original media cannot be recreated from database metadata.                          |
| AI retrieval                       | Current index/embeddings are in-process derived data. Rebuildable from approved Postgres sources in principle, workspace-scoped and deletion-aware; no production full-reindex command or measured duration exists.                         | Search/retrieval unavailable until rebuilt; source loss follows Postgres impact.            |
| Fly secrets/config                 | Irreplaceable deployment configuration; values were not read or recorded. Requires separately controlled escrow and rotation procedure.                                                                                                     | Apps cannot connect/decrypt/sign until restored.                                            |
| Stripe                             | External system of record for money and subscriptions. Reconcile customers/subscriptions/events into the restored entitlement mirror, oldest retained event first with idempotency.                                                         | Entitlements can be stale; Stripe remains authoritative.                                    |
| Auth0                              | External identity system of record. Reconcile Auth0 subjects and roles; Postgres cannot restore credentials.                                                                                                                                | Login/mapping failures.                                                                     |
| Postmark, Twilio, social providers | External delivery/account state. Reconcile provider logs where retained; messages and webhooks may not be replayable, and tokens may require reconnection.                                                                                  | Gaps or duplicates require provider-by-provider review.                                     |

## Recovery gaps

1. Enable and verify WAL/PITR with documented retention and earliest restore point.
2. Complete a paid, isolated Postgres restore drill and aggregate-only validation.
3. Confirm Upstash persistence/backup settings in its dashboard and test queue reconciliation.
4. Confirm Cloudinary original-asset backup/versioning, deletion retention, quota alerts, and a sample image/video recovery path.
5. Implement and test a workspace-scoped AI rebuild command with count/hash reconciliation and deletion replay.
6. Escrow and drill restoration of secrets and provider configuration without committing values.
